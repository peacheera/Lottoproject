package javabasic.oop.inheritance;

// 상속 받는 클래스
// 하위클래스 (sub) = 자식클래스(child) = 파생클래스(derived)


//	parent의 참조범위 = object ~ parent
//	child 참조범위는 object ~ parent ~ child
// 항상 하위클래스 객체의 참조범위는 상위클래스 객체의 참조범위보다 크다

public class Child extends Parent{

}
